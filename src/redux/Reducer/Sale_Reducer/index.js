const stateDefault = {

}

const ManagerSaleReducer = (state = stateDefault, { type, payload }) => {
    switch (type) {

        default:
            return { ...state }
    }
}

export default ManagerSaleReducer